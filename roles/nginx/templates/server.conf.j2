server {

    listen 80;
    server_name {{ my_domain_name }};

    # TEST: {{ my_domain_name|replace(".","-") }}
    
    # Include SSL configurations
    # include /etc/nginx/snippets/ssl-server.conf;
    
    # Dynamic location configurations
    include /etc/nginx/locations/*.conf;
}